/**
 * Type declaration for the `qrcode` module.
 *
 * This module does not provide its own TypeScript types,
 * so we declare it here to allow imports without type errors.
 *
 * Note: All imports from this module will be typed as `any`
 * unless more specific typings are added.
 */
declare module 'qrcode';

/**
 * Type declaration for the `topojson-client` module.
 *
 * This is a fallback declaration used when official type
 * definitions are not available or not installed.
 *
 * Note: Consider installing `@types/topojson-client` or
 * providing custom typings for better type safety.
 */
declare module 'topojson-client';
